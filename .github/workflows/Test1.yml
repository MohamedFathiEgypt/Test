on:
  schedule:
    - cron: '*/10   * * * *'

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Schedule_Query
        run: |
          current_date_time=$(date)
          echo "Current date and time: $current_date_time"
