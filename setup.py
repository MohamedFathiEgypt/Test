from setuptools import setup, find_packages

setup(
    name="pandas_ta_fork",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        "numpy==1.26.4",
        # add any other dependencies here
    ],
)
